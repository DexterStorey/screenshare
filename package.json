{
  "name": "screenshare",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/server.ts",
    "start": "NODE_ENV=production bun run src/server.ts",
    "build:client": "bun build ./src/client/broadcaster.ts ./src/client/viewer.ts --outdir ./public/js --minify --sourcemap=linked",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "bun-types": "^1.1.10",
    "typescript": "^5.6.3"
  },
  "engines": {
    "bun": ">=1.1.17"
  }
}

